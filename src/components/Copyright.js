const Copyright = () => {
  const currentYear = new Date().getFullYear();

  return <div className="text-sm text-center mb-2">© Copyright {currentYear} Fulbert Pognon</div>;
};

export default Copyright;
